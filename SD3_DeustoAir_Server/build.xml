<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project basedir="." default="help" name="SD3_DeustoAir_Server">
    <property environment="env"/>
	
	<property name="source.dir" location="src"/>
	<property name="build.dir" location="bin"/>
	<property name="dist.dir" location="dist"/>
	<property name="lib.dir" location="lib"/>
	<property name="classpath" location="bin"/>
	<property name="export.file" value="xxxx.jar"/>
	<property name="server.port" value="1099"/> 
	<property name="server.IP" value="127.0.0.1"/> 
	<property name="server.Name" value="Vodafone"/>
	<property name="policy.file" location="security/java.policy"/>
              	
    <path id="full.classpath">
    	<fileset dir="${lib.dir}">
        	<include name="**/*.jar"/>
       </fileset>
       <pathelement location="${build.dir}"/>    	
    </path>

	<target name="help">
		<echo>Usage: ant [build | export | server ]</echo>
	</target>
	
    <target name="build" description="Builds the project">
    	<delete dir="${build.dir}"/>
    	<mkdir dir="${build.dir}"/>
    	<javac srcdir="${source.dir}" destdir="${build.dir}" classpath="${classpath}" deprecation="true" fork="true" classpathref="full.classpath" includeantruntime="no"/>
    </target>
              	
    <target name="export" depends="build" description="Export the project into a .jar file">
    	<delete dir="${dist.dir}"/>
    	<mkdir dir="${dist.dir}"/>
    	<jar destfile="${dist.dir}/${export.file}" basedir="${build.dir}" includes=""/>
    </target>
	
	<target name="server" depends="build" description="Runs Server program">
		<java classname="" classpath="${classpath}" fork="true" classpathref="full.classpath">
	        <jvmarg value="-Djava.rmi.server.codebase=file:${build.dir}/"/>
			<jvmarg value="-Djava.security.policy=${policy.file}"/>			
	        <arg value="${server.IP}"/> 
		 	<arg value="${server.port}"/> 
			<arg value="${server.Name}"/> 
		</java>
	</target>
    
</project>
